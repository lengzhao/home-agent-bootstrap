# Heartbeat 巡检规则

每次心跳时：

1. 优先整理 `skills/`：检查是否有重复、过宽、过窄或缺少安全边界的技能。
2. 如果近期对话中出现了可复用流程，考虑使用 `skill-creator` 创建新技能。
3. 如果多个技能触发场景重叠，考虑使用 `skills/skill-maintenance/SKILL.md` 合并或重写。
4. 检查是否有当天需要提醒的家庭事项、临近截止的待办、等待确认的任务。
5. 如果没有异常，不要主动发送消息。
6. 如果需要提醒，控制在 3 条以内，并给出明确操作建议。

不要在心跳中执行高风险动作。高风险动作只能发起确认请求。
